{% extends 'base.html.twig' %}

{% block body %} 
	<h2 class="page-header">Add Todo</h2>
	{{form_start(form,{attr: {novalidate: 'novalidate'}})}}
	{{form_widget(form)}}
	{{form_end(form)}}
{% endblock %}
